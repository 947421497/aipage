# 三级菜单改造方案设计

## 一、项目背景与目标

### 1.1 项目概述

本项目是基于一鱼PHP框架（XPHP Framework）开发的后台管理系统，采用经典的MVC架构模式，前端使用Bootstrap 5和jQuery构建响应式管理界面。当前后台菜单系统采用扁平化结构，所有菜单项均为一级菜单，缺乏层级关系和折叠交互功能，无法满足复杂业务系统的导航需求。

### 1.2 现状分析

#### 1.2.1 当前菜单表结构

当前菜单表`xphp_menu`采用单表设计，字段包括：菜单ID、标题、链接地址、标识符、图标、系统标识、排序值、更新时间、状态。表结构简洁但缺乏层级表达能力，所有菜单记录在同一平面上，通过sort字段控制显示顺序。这种设计在菜单数量较少时足够使用，但当系统功能模块增多、需要分组展示时，扁平结构的局限性就会显现出来。

#### 1.2.2 当前前端实现

侧边栏模板`sidebar.html`通过Widget组件获取全部启用状态的菜单，以简单的无序列表形式渲染。每个菜单项都是独立的链接，点击即跳转。这种实现方式存在以下问题：没有菜单分组概念，无法表达功能模块的归属关系；没有折叠展开功能，所有菜单始终平铺展示，占用大量屏幕空间；没有视觉层级区分，一级菜单和功能入口混在一起，用户难以快速定位目标功能。

#### 1.2.3 当前权限控制

权限验证中间件`CpAuth`仅检查用户是否登录以及用户等级是否达到3级，没有与菜单进行关联验证。这意味着任何具有后台访问权限的用户都能看到所有菜单，无法实现基于角色的菜单可见性控制。

### 1.3 改造目标

本次改造的核心目标是将现有的扁平菜单系统升级为支持三级嵌套的树形菜单系统，并实现手风琴折叠效果和层级样式区分。具体目标包括：支持一级父菜单、二级子菜单、三级功能菜单的完整嵌套结构；实现点击一级菜单时展开或折叠其下的二级菜单，同一时间只展开一个一级菜单的手风琴效果；为不同层级菜单设计差异化的视觉样式，通过缩进、图标、背景色等元素区分菜单层级；保持与现有系统的向后兼容，确保不影响现有功能的前提下完成改造。

## 二、需求分析

### 2.1 功能性需求

#### 2.1.1 三级菜单层级定义

第一级菜单作为功能模块的入口，通常对应系统中相对独立的大功能区域，如用户管理、内容管理、系统设置等。一级菜单不需要填写跳转链接（href字段可为空），但必须有菜单标题、唯一标识和图标。一级菜单的主要作用是分组归纳下级功能，其本身不直接对应具体页面。

第二级菜单作为功能子模块的入口，对应一级菜单下的具体功能分类。例如在“用户管理”这一级菜单下，可能包含“用户列表”、“角色管理”、“权限分配”等二级菜单。二级菜单需要有跳转链接，用户点击后可进入对应功能页面。

第三级菜单作为具体功能操作的入口，是菜单树的最底层，对应最终的操作页面。例如在“用户列表”这一二级菜单下，可能包含“查看用户”、“编辑用户”、“删除用户”等三级菜单（虽然实际项目中可能将这些作为按钮而非独立菜单，此处仅为层级示例）。三级菜单需要有跳转链接，点击后跳转到具体功能页面。

#### 2.1.2 手风琴交互效果

手风琴效果是指在同一时间只展开一个一级菜单的二级菜单列表。当用户点击某个一级菜单时，如果此时有其他一级菜单的二级菜单处于展开状态，则自动折叠这些已展开的二级菜单；如果当前点击的一级菜单已经展开，则再次点击将其折叠。用户应能清晰感知当前展开的是哪个功能模块，便于快速定位和切换。

#### 2.1.3 菜单数据管理

需要提供菜单的增删改查功能，支持选择父级菜单。添加新菜单时应能选择该菜单的父级（顶级菜单则不选择父级），系统应限制最多支持三级嵌套，即只能为二级菜单选择三级菜单作为子菜单，不能无限嵌套。菜单列表应展示层级关系，可通过缩进或树形结构清晰展示上下级关系。

### 2.2 非功能性需求

#### 2.2.1 性能要求

菜单数据的读取和树形结构构建应在服务器端完成，避免在前端进行复杂的递归处理。菜单数据应支持缓存，避免每次请求都查询数据库。菜单树构建算法应高效，对于百级菜单应能在毫秒级完成处理。

#### 2.2.2 兼容性要求

改造应保持与现有系统的向后兼容。现有的单级菜单数据应能平滑迁移到新的三级结构中。菜单管理界面的操作习惯应与现有后台保持一致，降低用户学习成本。

#### 2.2.3 可维护性要求

菜单层级关系应通过数据字段明确表达，便于后续扩展和维护。菜单渲染逻辑应模块化，便于根据需求调整样式和交互。代码应遵循框架的编码规范，保持与项目其他部分的一致性。

## 三、技术方案设计

### 3.1 数据库改造

#### 3.1.1 表结构变更

在现有`xphp_menu`表基础上新增`pid`字段（父级ID），用于建立菜单项之间的父子关系。该字段为无符号小整数类型，默认为0表示顶级菜单。为支持更深的层级扩展和未来可能的四级菜单需求，字段类型选用`smallint unsigned`而非`tinyint unsigned`。

具体字段变更如下：将`id`字段类型从`smallint(5) unsigned`修改为`smallint(5) unsigned`，保持不变但建议在注释中明确最大菜单数量限制；新增`pid`字段，类型为`smallint(5) unsigned`，默认值为0，注释为“父级ID，0表示顶级菜单”；现有数据中`pid`字段默认值设为0，保持原有菜单的一级菜单属性。

修改后的建表语句如下：

```sql
CREATE TABLE `xphp_menu` (
  `id` smallint(5) unsigned NOT NULL AUTO_INCREMENT COMMENT '菜单ID',
  `pid` smallint(5) unsigned NOT NULL DEFAULT '0' COMMENT '父级ID，0表示顶级菜单',
  `title` varchar(50) NOT NULL DEFAULT '' COMMENT '菜单标题',
  `href` varchar(100) NOT NULL DEFAULT '' COMMENT '链接地址，空表示父级菜单',
  `sign` varchar(20) NOT NULL DEFAULT '' COMMENT '菜单标识',
  `icon` varchar(100) NOT NULL DEFAULT '' COMMENT '菜单图标',
  `is_sys` tinyint(1) unsigned NOT NULL DEFAULT '0' COMMENT '系统菜单(0可删/1禁删)',
  `sort` int(11) unsigned NOT NULL DEFAULT '0' COMMENT '排序权重',
  `update_time` int(10) unsigned NOT NULL DEFAULT '0' COMMENT '更新时间',
  `status` tinyint(1) unsigned NOT NULL DEFAULT '0' COMMENT '状态(0正常/1禁用)',
  PRIMARY KEY (`id`),
  KEY `idx_pid` (`pid`),
  KEY `idx_status` (`status`),
  KEY `idx_sign` (`sign`)
) ENGINE=InnoDB AUTO_INCREMENT=6 DEFAULT CHARSET=utf8mb4 COMMENT='菜单表';
```

#### 3.1.2 索引设计

新增`pid`字段后应创建索引以优化基于父级的查询性能。新增的索引包括：`idx_pid`索引用于加速查询某父级下的所有子菜单；保留原有的`idx_sign`唯一索引确保菜单标识唯一性；新增`idx_status`索引用于筛选正常状态的菜单记录。

#### 3.1.3 数据迁移策略

现有菜单数据的`pid`字段统一设置为0，表示这些菜单都是顶级菜单。迁移过程中不对现有数据做任何修改，只新增字段并设置默认值。这样可以确保迁移过程安全可靠，即使出现问题也可以快速回滚。

### 3.2 后端逻辑改造

#### 3.2.1 菜单Widget组件改造

现有的菜单Widget组件`app/admin/widget/Menu.php`需要改造为支持树形结构构建。改造后的组件应从数据库获取全部启用状态的菜单记录，然后在PHP端完成树形结构的递归构建，最后将构建好的树形数据返回给视图层。

树形构建算法的核心思路是：首先将全部菜单数据按ID建立索引，形成关联数组；然后遍历所有菜单，将每个菜单的引用存入以其父级ID为键的临时数组中；最后从PID为0的顶级菜单开始，递归地将子菜单挂载到父级菜单的children属性下。

改造后的Widget代码逻辑应包含以下要点：设置缓存有效期为0表示不缓存，确保菜单变更能即时生效；查询条件为`status=1`表示只获取启用状态的菜单；查询结果按`sort ASC, id ASC`排序，确保同级菜单按设定的顺序显示；树形构建过程在服务端完成，减轻前端处理负担。

#### 3.2.2 菜单模型验证规则调整

菜单模型`app/admin/model/Menu.php`的自动验证规则需要相应调整。对于`href`字段的验证规则，一级菜单（PID为0）的href可以为空，而二级和三级菜单的href必须符合`控制器/方法`的格式。建议将验证规则修改为条件验证：一级菜单不验证href格式，二级和三级菜单验证href格式为非空且符合规范。

对于`sign`字段的验证规则，应确保同一父级下不存在重复的sign值。可以考虑添加自定义验证器实现父子级内的唯一性检查。

#### 3.2.3 菜单控制器增强

菜单控制器`app/admin/controller/Menu.php`继承自Cp基类，基本的CRUD功能由基类提供。需要在菜单控制器中添加父级菜单的选择逻辑，为添加和编辑页面提供父级菜单列表数据。可以通过重写add和edit方法，在返回视图前查询并传递父级菜单数据。

### 3.3 前端界面改造

#### 3.3.1 侧边栏模板改造

侧边栏模板`app/admin/view/public/sidebar.html`需要完全重写，从扁平列表改为嵌套的树形结构。改造后的模板应使用多层无序列表嵌套，外层ul表示菜单容器，内层li表示单个菜单项，li内部的ul表示该菜单的子菜单。

模板结构设计如下：最外层导航容器使用`.nav-drawer`类；每个菜单项li包含两部分内容，分别是菜单链接（包含图标和标题）和可选的子菜单容器（ul.children）；顶级菜单li添加`.has-submenu`类表示含有子菜单；二三级菜单li通过CSS缩进和样式差异表现层级关系。

具体模板结构示例：

```html
<ul class="nav-drawer">
  <li class="nav-item{:nav_active('index', ' active')}">
    <a href="{:url('index/index')}">
      <i class="mdi mdi-home-city-outline"></i>
      <span>管理中心</span>
    </a>
  </li>
  {php $menu = widget('menu')->get()}
  {foreach $menu as $nav}
  <li class="nav-item has-submenu{:nav_active($nav['sign'], ' active open')}">
    <a href="javascript:;" class="submenu-toggle" data-sign="{$nav.sign}">
      <i class="{$nav.icon}"></i>
      <span>{$nav.title}</span>
      <i class="mdi mdi-chevron-down submenu-arrow"></i>
    </a>
    <ul class="nav-submenu">
      {foreach $nav.children as $child}
      <li class="nav-item{:nav_active($child['sign'], ' active')}">
        <a href="{:url($child.href)}">
          <span>{$child.title}</span>
        </a>
        {if !empty($child.children)}
        <ul class="nav-submenu-level3">
          {foreach $child.children as $grandchild}
          <li class="nav-item{:nav_active($grandchild['sign'], ' active')}">
            <a href="{:url($grandchild.href)}">
              <span>{$grandchild.title}</span>
            </a>
          </li>
          {/foreach}
        </ul>
        {/if}
      </li>
      {/foreach}
    </ul>
  </li>
  {/foreach}
</ul>
```

#### 3.3.2 菜单样式设计

不同层级菜单应通过样式实现视觉区分。一级菜单项的样式应保持原有风格，使用较大的图标和粗体标题，与“管理中心”保持一致的视觉重量；左侧padding设置为标准尺寸，不需要额外缩进。

二级菜单项应通过左侧增加缩进来表现层级关系，padding-left值应比一级菜单多16至24像素；字体大小可略小于一级菜单；图标尺寸可缩小或省略，使用圆点或短横线等小图标代替。

三级菜单项的缩进应比二级更多，padding-left值继续增加16至24像素；字体大小可进一步缩小；背景色可使用更浅的色调以区分层级。

选中状态样式应继承Bootstrap的active类效果，使用主题色背景和白色文字。高亮当前菜单的逻辑可通过`nav_active`助手函数实现，该函数根据当前请求的控制器和方法判断是否应添加active类。

#### 3.3.3 手风琴交互实现

手风琴效果通过JavaScript实现，核心逻辑是点击一级菜单时先关闭其他已展开的一级菜单，然后切换当前菜单的展开状态。具体实现步骤如下：

绑定点击事件：为所有`.submenu-toggle`元素绑定点击事件处理器。当用户点击带有子菜单的一级菜单标题时，触发事件处理函数。

事件处理逻辑：首先获取被点击菜单的DOM元素；然后遍历所有同级菜单，移除具有展开状态class的元素的展开样式；接着判断被点击菜单原本是否处于展开状态；如果原本已展开则添加折叠class移除展开class，如果原本未展开则添加展开class移除折叠class；最后阻止链接的默认跳转行为。

可选的Cookie或LocalStorage记录功能可记住用户上次展开的菜单，在页面刷新后自动恢复展开状态。这需要在前端存储当前展开的菜单sign值，并在页面加载时根据存储的值恢复展开状态。

### 3.4 菜单管理界面改造

#### 3.4.1 菜单列表页面

菜单列表页面`app/admin/view/menu/index.html`需要调整表格列以展示层级关系。可通过以下方式实现层级展示：在标题列前增加父级菜单列，显示每个菜单的父级菜单名称；或者保持现有表格结构不变，在标题列中通过缩进和前缀（如"├─"）表现层级关系。

列表排序应调整为按`pid ASC, sort ASC, id ASC`排序，确保同一父级下的子菜单相邻显示，并且按设定的顺序排列。

#### 3.4.2 添加菜单页面

添加页面`app/admin/view/menu/add.html`需要新增父级菜单选择字段。该字段应使用下拉选择框实现，选项包括“顶级菜单”和所有现有二级菜单。顶级菜单的PID为0，二级菜单的PID为已存在的顶级菜单ID。

下拉选项应按层级分组显示：一级选项显示所有顶级菜单；二级选项显示顶级菜单下的二级菜单（可添加缩进前缀区分）。用户选择某一级菜单后，可在该菜单下添加三级菜单；用户选择某二级菜单后，只能添加该菜单的子菜单；用户选择顶级菜单，则添加一级菜单。

菜单层级限制逻辑在服务端实现，后端API应拒绝创建超过三级的菜单。当用户选择二级菜单作为父级时，提交表单后服务端检查该二级菜单的父级是否为顶级菜单（即二级菜单的PID是否不为0），如果是则拒绝创建新菜单。

#### 3.4.3 编辑菜单页面

编辑页面`app/admin/view/menu/edit.html`同样需要增加父级菜单选择字段。默认值应设置为当前菜单的PID值。需要注意的是，编辑菜单时不允许将菜单的父级设置为自己或自己的子级，以避免循环引用问题。服务端应验证提交的父级ID不是当前菜单ID，也不属于当前菜单的子菜单树。

## 四、权限控制设计

### 4.1 权限模型扩展

当前系统使用简单的用户等级（level字段）进行权限控制，三级及以上用户可登录后台。这种粗粒度的权限控制无法实现菜单级别的权限管理。

建议扩展方案是：为用户表增加菜单权限字段，存储该用户可见的菜单ID列表。在用户登录时将权限菜单列表加载到Session中。菜单Widget在构建菜单树时，根据Session中的权限列表过滤菜单记录，只返回用户有权限访问的菜单。

### 4.2 菜单级权限实现

菜单级权限控制的核心是在服务端过滤用户可见的菜单。具体实现方式如下：在Widget组件中增加权限过滤逻辑，从Session中读取当前用户的菜单权限列表；如果用户有权限限制（权限列表非空），则在查询菜单时使用`whereIn`条件过滤；构建树形结构时，如果某个父级菜单的子菜单全部被过滤掉，则该父级菜单也不显示。

对于权限列表为空的用户（通常为超级管理员），不过滤菜单，显示全部菜单。这种设计确保了权限控制的灵活性和扩展性。

## 五、数据结构设计

### 5.1 菜单树形数据结构

服务端构建的菜单树形数据结构应采用标准的树形节点格式。每个节点包含以下属性：id表示菜单唯一标识；pid表示父级菜单ID，0表示顶级菜单；title表示菜单显示标题；href表示菜单链接地址，空字符串表示父级菜单；sign表示菜单标识符，用于高亮当前菜单；icon表示菜单图标类名；children表示子菜单数组，没有子菜单时为空数组。

返回给前端的数据结构示例：

```php
[
    [
        'id' => 1,
        'pid' => 0,
        'title' => '用户管理',
        'href' => '',
        'sign' => 'user',
        'icon' => 'mdi mdi-account',
        'children' => [
            [
                'id' => 5,
                'pid' => 1,
                'title' => '用户列表',
                'href' => 'user/index',
                'sign' => 'user_list',
                'icon' => '',
                'children' => []
            ],
            [
                'id' => 6,
                'pid' => 1,
                'title' => '角色管理',
                'href' => 'role/index',
                'sign' => 'role',
                'icon' => '',
                'children' => []
            ]
        ]
    ],
    [
        'id' => 2,
        'pid' => 0,
        'title' => '系统设置',
        'href' => '',
        'sign' => 'config',
        'icon' => 'mdi mdi-cog',
        'children' => [
            [
                'id' => 7,
                'pid' => 2,
                'title' => '网站配置',
                'href' => 'config/index',
                'sign' => 'config_index',
                'icon' => '',
                'children' => []
            ]
        ]
    ]
]
```

### 5.2 菜单项编辑数据结构

菜单添加和编辑表单提交的数据应包含以下字段：title（菜单标题，必填）；pid（父级ID，必填，0表示顶级菜单）；href（链接地址，二三级菜单必填，一级菜单可为空）；sign（菜单标识，必填，唯一）；icon（图标类名，可空）；sort（排序值，可空，默认为100）；is_sys（是否系统菜单，可空，默认为否）。

表单验证规则：一级菜单（pid=0）时href可为空；二级和三级菜单时href必须符合`控制器/方法`格式且不能为空；sign在同级菜单下必须唯一。

## 六、文件变更清单

### 6.1 数据库文件

需要修改的数据库相关文件包括：`backup/bak_all_initialize/2_create_table.sql`文件需要添加pid字段的表结构定义，作为安装初始化使用；`backup/bak_all_initialize/3_insert_xphp_menu_part1.sql`文件中的菜单数据需要调整，为现有菜单设置pid=0（顶级菜单）。

### 6.2 PHP后端文件

需要修改的PHP后端文件包括：`app/admin/model/Menu.php`文件需要调整href字段的验证规则以支持一级菜单href为空；`app/admin/widget/Menu.php`文件需要重写菜单数据获取逻辑，增加树形结构构建功能；`app/admin/controller/Menu.php`文件需要增加获取父级菜单列表的方法，为表单提供父级选择数据。

### 6.3 视图模板文件

需要修改的视图模板文件包括：`app/admin/view/public/sidebar.html`文件需要完全重写，实现三级嵌套菜单结构和手风琴交互；`app/admin/view/menu/index.html`文件需要调整列表展示，可选增加父级菜单列或层级缩进显示；`app/admin/view/menu/add.html`文件需要增加父级菜单选择下拉框；`app/admin/view/menu/edit.html`文件需要增加父级菜单选择下拉框并设置当前值为默认选项。

### 6.4 静态资源文件

需要新增或修改的静态资源文件包括：`public/static/css/style.min.css`文件需要增加三级菜单的CSS样式定义，包括层级缩进、展开折叠箭头图标等；`public/static/js/main.min.js`文件需要增加手风琴交互的JavaScript代码。

## 七、测试验证计划

### 7.1 功能测试用例

功能测试应覆盖以下场景：添加顶级菜单并验证其正确显示在侧边栏顶部；添加二级菜单并验证其正确挂载到对应的一级菜单下；添加三级菜单并验证其正确挂载到对应的二级菜单下；验证最多只能添加三级菜单，无法创建四级菜单；验证手风琴效果，点击一级菜单时其他已展开的菜单正确折叠；验证当前菜单高亮功能，根据当前访问的URL正确显示active状态。

### 7.2 数据完整性测试

数据完整性测试应覆盖以下场景：编辑菜单的父级时验证不能设置为自身或子级；删除菜单时验证其子菜单的处理方式（可选择一并删除或阻止删除）；禁用菜单时验证其子菜单的显示状态。

### 7.3 兼容性测试

兼容性测试应覆盖以下场景：现有数据库迁移后原有菜单是否正常显示；新增菜单功能与现有功能是否冲突；不同浏览器下菜单样式和交互是否正常。

## 八、实施注意事项

### 8.1 数据库修改范围

本次改造的数据库修改仅涉及备份文件，不直接修改生产数据库。用户需通过重新安装系统的方式完成数据库初始化。如果系统已上线运行，需要先备份现有数据库，然后导出菜单数据，修改备份SQL文件，执行修改后的SQL完成数据库升级。

### 8.2 向后兼容性

菜单表新增的pid字段默认值为0，这意味着现有菜单记录在改造后仍被视为顶级菜单，不需要额外的数据迁移工作。菜单Widget的get方法返回数据结构从一维数组变为嵌套的树形数组，前端模板需要相应调整以适配新数据结构。

### 8.3 性能考虑

树形结构构建在服务端完成后一次性返回，减少前端处理量。菜单数据可根据实际需求决定是否启用缓存，在菜单变更频繁时建议保持不缓存以确保实时性。

### 8.4 扩展性预留

虽然本次改造明确限制为三级菜单，但数据库字段设计已为可能的四级菜单扩展预留空间。如果未来需要支持更多层级，只需修改前端模板和菜单层级限制逻辑，数据库结构无需变更。
